global:
  scrape_interval: 15s

  evaluation_interval: 15s


scrape_configs:

  # =========================================================
  # Airflow metrics
  # =========================================================

  - job_name: airflow

    static_configs:
      - targets:
          - statsd-exporter:9102